The static content of the web site (HTML, JSP, JavaScript, CSS, and images) that will be 
accessible to application clients.  This directory will be the 'document root' of the web 
application, and any subdirectory structure found here will be reflected in the request URIs 
required to access those files.