Java source files that generate the servlets, beans, and other Java classes that are unique to the web application.  Source
code should be organized in packages, and the package hierarchy should be reflected as a directory structure underneath
this directory.